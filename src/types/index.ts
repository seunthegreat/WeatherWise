export interface Component {
  
}

export interface Constant {
  City: {
    id: number;
    name: string;
    lngLat: number[];
  }
};

export interface Styling {
  Text: {
    normal: string;
    small: string;
    smallest: string;
    heading: string;
    subHeading: string;
    body: string;
  }
}